# MSW (Mock Service Worker)

Run `pnpm install` to install dependencies.

Run `pnpm dev` to start the development server.

## Deployment

App is deployed at GitHub Pages: https://psea.github.io/msw-hidden-timeout
